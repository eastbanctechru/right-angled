import { RouterConfig } from '@angular/router';

import { GroupingSampleComponent }   from './grouping-sample.component';

export const GroupingSampleRoutes: RouterConfig = [
    {
        component: GroupingSampleComponent,
        path: 'grouping'
    }
];
